# FastAPI and server
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
pydantic>=2.5.0,<3.0.0

# HTTP client
aiohttp>=3.9.0,<4.0.0

# Environment variables
python-dotenv>=1.0.0,<2.0.0

# Supabase client for storage operations
supabase>=2.0.0,<3.0.0

# Whisper transcription via CTranslate2 — no PyTorch needed, ~70 MB RAM (vs ~350 MB for torch+openai-whisper)
faster-whisper>=1.0.0,<2.0.0

# YouTube video downloading. Deliberately NOT upper-bounded — yt-dlp ships frequent
# releases to keep up with YouTube extractor changes; pinning a ceiling here would
# block those fixes from being picked up.
yt-dlp>=2024.1.0

# Note: FFmpeg must be installed separately on the system
# For Ubuntu/Debian: sudo apt-get install ffmpeg
# For macOS: brew install ffmpeg
# For Windows: Download from https://ffmpeg.org/download.html

# LangGraph for workflow orchestration.
# Deliberately NOT upper-bounded here: this ecosystem ships frequent breaking major
# releases and I couldn't verify which major versions are actually deployed/tested
# (the local venv found in this repo doesn't even have these installed). Before
# capping these, run `pip freeze` against the real deployment venv and pin exact
# versions (==) instead of guessing a ceiling that might already be violated.
langgraph>=0.2.59
langchain-anthropic>=0.3.0
langchain-core>=0.3.28

# Logo overlay: Pillow renders the AM logo geometry in pure Python (no native libs needed)
Pillow>=10.0.0,<12.0.0
