# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# HTTP client
aiohttp>=3.9.0

# Environment variables
python-dotenv>=1.0.0

# Supabase client for storage operations
supabase>=2.0.0

# Whisper transcription via CTranslate2 — no PyTorch needed, ~70 MB RAM (vs ~350 MB for torch+openai-whisper)
faster-whisper>=1.0.0

# Note: FFmpeg must be installed separately on the system
# For Ubuntu/Debian: sudo apt-get install ffmpeg
# For macOS: brew install ffmpeg
# For Windows: Download from https://ffmpeg.org/download.html

# LangGraph for workflow orchestration
langgraph>=0.2.59
langchain-openai>=0.2.14
langchain-core>=0.3.28

# Server-Sent Events support
sse-starlette>=2.1.3

# Logo overlay: Pillow renders the AM logo geometry in pure Python (no native libs needed)
Pillow>=10.0.0
